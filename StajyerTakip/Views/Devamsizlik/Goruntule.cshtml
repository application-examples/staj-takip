@model Stajyer
@{
    ViewData["Title"] = "Goruntule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<input type="text" hidden="hidden" id="#stajyerid" value="@Model.ID" />
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th> </th>
                            @for (int i = 1; i <= 31; i++)
                            {
                                <th class="font-10 text-info">@i</th>
                            }

                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>Ocak</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "ocak" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Şubat</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "subat" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Mart</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "mart" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Nisan</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "nisan" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Mayıs</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "mayis" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Haziran</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "haziran" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Temmuz</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "temmuz" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Ağustos</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "agustos" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Eylül</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "eylul" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Ekim</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "ekim" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Kasım</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "kasim" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                        <tr>
                            <td>Aralık</td>
                            @for (int i = 1; i <= 31; i++)
                            {
                                var id = "aralik" + i;
                                <td id="@id"></td>
                            }
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<script>

    $(function () {
        $.ajax({
            url: '/Devamsizlik/Devamsizliklar',
            data: { id: $('#stajyerid').val() },
            dataType: 'json',
            type: 'POST',
            success: function (data) {
                $.each(data, function (id, obj) {
                    console.log(obj.tarih+'as');
                    var tarih = obj.tarih.toString().split('');
                });
            }
        });
    });

</script>